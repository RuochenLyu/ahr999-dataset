name: Daily update & deploy

on:
  schedule:
    # 00:37 UTC — 37 min after Binance closes the previous UTC day bar.
    # Off-the-hour minute to avoid queue contention near UTC midnight.
    - cron: '37 0 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - 'src/**'
      - 'scripts/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/daily.yml'

concurrency:
  group: daily-and-pages
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Sync dataset + export CSV
        if: github.event_name != 'push'
        run: pnpm run update

      - name: Verify dataset invariants
        run: pnpm verify

      - name: Commit dataset if changed
        if: github.event_name != 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet datasets/; then
            echo "No dataset changes."
          else
            git add datasets/
            git commit -m "chore(data): update $(date -u -I)"
            git push
          fi

      - name: Copy datasets into web/public
        run: pnpm run web:prepublic

      - name: Install web dependencies
        run: pnpm --filter ./web install --frozen-lockfile

      - name: Build Astro site
        run: pnpm --filter ./web build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

      - id: deployment
        uses: actions/deploy-pages@v4
